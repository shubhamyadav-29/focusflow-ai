// export const summarizeNoteAPI = (note) => {
//   return new Promise((resolve) => {
//     setTimeout(() => {
//       const summary = note.length > 20 
//         ? note.slice(0, 20) + "..."
//         : note

//       resolve({ data: summary })
//     }, 1000)
//   })
// }